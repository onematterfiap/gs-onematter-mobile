import { StyleSheet, Text, View } from "react-native";
const NotFound = () => {
    return (
        <View>
            <Text>NotFound</Text>
        </View>
    );
};
export default NotFound;
const styles = StyleSheet.create({});
